<?xml version="1.0" encoding="utf-8"?>

<LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:orientation="vertical"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:nextFocusLeft="@null"
        android:nextFocusRight="@null"
        android:nextFocusUp="@+id/tv_tab_favor">


    <!--<GridView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingTop="20dp"
            android:paddingLeft="10dp"
            android:paddingRight="10dp"
            android:paddingBottom="10dp"
            android:id="@+id/favorGridView"
            android:verticalSpacing="48dp"
            android:scrollbars="none"
            android:horizontalSpacing="24dp"
            android:numColumns="4"
            android:nextFocusLeft="@null"
            android:nextFocusRight="@null">

    </GridView>-->

    <ScrollView
            android:id="@+id/scroll_favor"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            >

        <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical"
                android:layout_marginLeft="@dimen/main_tab_padding_left_right"
                android:layout_marginRight="@dimen/main_tab_padding_left_right"
                android:id="@+id/favorGridView"
                />
    </ScrollView>
</LinearLayout>